import { SortGoalsByPercentagesPipe } from './sort-goals-by-percentages.pipe';

describe('SortGoalsByPercentagesPipe', () => {
  it('create an instance', () => {
    const pipe = new SortGoalsByPercentagesPipe();
    expect(pipe).toBeTruthy();
  });
});
